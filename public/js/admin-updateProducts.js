document.addEventListener("DOMContentLoaded", () => {    try {      if (typeof locals !== "undefined" && locals.success_msg && locals.success_msg.length > 0) {        Swal.fire({          icon: "success",          title: "Success",          text: locals.success_msg,          confirmButtonColor: "#0d6efd",        })      }      if (typeof locals !== "undefined" && locals.error_msg && locals.error_msg.length > 0) {        Swal.fire({          icon: "error",          title: "Error",          text: locals.error_msg,          confirmButtonColor: "#0d6efd",        })      }    } catch (e) {  }    let deletedImages = []    const cropperModalEl = document.getElementById("imageCropperModal")    const cropperModal = new bootstrap.Modal(cropperModalEl)    let cropper    let currentImageInput    let currentImageContainer    let currentRemoveButton    let currentOldImageInput    const cropperImage = document.getElementById("cropperImage")    const rotateLeftBtn = document.getElementById("rotateLeftBtn")    const rotateRightBtn = document.getElementById("rotateRightBtn")    const zoomInBtn = document.getElementById("zoomInBtn")    const zoomOutBtn = document.getElementById("zoomOutBtn")    const resetBtn = document.getElementById("resetBtn")    const cropImageBtn = document.getElementById("cropImageBtn")    function showError(inputElement, errorMessage) {      const errorElement = document.getElementById(inputElement.id + "Error")      if (errorElement) {        errorElement.textContent = errorMessage        errorElement.classList.add("show")        inputElement.classList.add("is-invalid")      }    }    function clearError(inputElement) {      const errorElement = document.getElementById(inputElement.id + "Error")      if (errorElement) {        errorElement.textContent = ""        errorElement.classList.remove("show")        inputElement.classList.remove("is-invalid")      }    }    function clearAllErrors() {      const errorElements = document.querySelectorAll(".invalid-feedback")      const invalidInputs = document.querySelectorAll(".is-invalid")      errorElements.forEach((el) => {        el.textContent = ""        el.classList.remove("show")      })      invalidInputs.forEach((input) => {        input.classList.remove("is-invalid")      })    }    function initializeImageUploader(inputId, containerId, removeBtnId, btnId, oldImageInputName) {      const inputElement = document.getElementById(inputId)      const containerElement = document.getElementById(containerId)      const removeBtnElement = document.getElementById(removeBtnId)      const btnElement = document.getElementById(btnId)      const oldImageInput = oldImageInputName ? document.querySelector(`input[name="${oldImageInputName}"]`) : null      if (!inputElement || !containerElement || !removeBtnElement) {        return      }      if (btnElement) {        btnElement.addEventListener("click", (e) => {          e.preventDefault()          e.stopPropagation()          inputElement.click()        })      }      containerElement.addEventListener("click", (e) => {        if (          (!btnElement || (e.target !== btnElement && !btnElement.contains(e.target))) &&          e.target !== removeBtnElement &&          !removeBtnElement.contains(e.target)        ) {          inputElement.click()        }      })      inputElement.addEventListener("change", () => {        if (inputElement.files && inputElement.files[0]) {          currentImageInput = inputElement          currentImageContainer = containerElement          currentRemoveButton = removeBtnElement          currentOldImageInput = oldImageInput          const reader = new FileReader()          reader.onload = (e) => {            cropperImage.src = e.target.result            cropperModal.show()            cropperModalEl.addEventListener(              "shown.bs.modal",              function initCropper() {                if (cropper) {                  cropper.destroy()                }                cropper = new Cropper(cropperImage, {                  aspectRatio: 4 / 5,                  viewMode: 1,                  dragMode: "move",                  autoCropArea: 0.8,                  restore: false,                  guides: true,                  center: true,                  highlight: false,                  cropBoxMovable: true,                  cropBoxResizable: true,                  toggleDragModeOnDblclick: false,                })                cropperModalEl.removeEventListener("shown.bs.modal", initCropper)              },              { once: true },            )          }          reader.readAsDataURL(inputElement.files[0])        }      })      removeBtnElement.addEventListener("click", (e) => {        e.preventDefault()        e.stopPropagation()        if (oldImageInput && oldImageInput.value) {          deletedImages.push(oldImageInput.value)          }        inputElement.value = ""        containerElement.innerHTML = `          <i class="fas fa-image upload-icon"></i>          <p class="upload-text">Drag and drop image here, or click to add image</p>          <button type="button" class="btn-upload" id="${btnId}">Add Image</button>        `        const newBtn = document.getElementById(btnId)        if (newBtn) {          newBtn.addEventListener("click", (e) => {            e.preventDefault()            e.stopPropagation()            inputElement.click()          })        }        removeBtnElement.classList.add("d-none")        if (oldImageInput) {          oldImageInput.setAttribute("data-removed", "true")        }        clearError(inputElement)      })      containerElement.addEventListener("dragover", (e) => {        e.preventDefault()        containerElement.style.borderColor = "#0d6efd"      })      containerElement.addEventListener("dragleave", (e) => {        e.preventDefault()        containerElement.style.borderColor = "#dee2e6"      })      containerElement.addEventListener("drop", (e) => {        e.preventDefault()        containerElement.style.borderColor = "#dee2e6"        if (e.dataTransfer.files && e.dataTransfer.files[0]) {          inputElement.files = e.dataTransfer.files          const changeEvent = new Event("change", { bubbles: true })          inputElement.dispatchEvent(changeEvent)        }      })    }    initializeImageUploader("mainImage", "mainPhotoUpload", "removeMainImage", "mainImageBtn", "oldMainImage")    initializeImageUploader(      "additionalImage1",      "additionalPhoto1Container",      "removeAdditionalImage1",      "additionalImage1Btn",      "oldAdditionalImage1",    )    initializeImageUploader(      "additionalImage2",      "additionalPhoto2Container",      "removeAdditionalImage2",      "additionalImage2Btn",      "oldAdditionalImage2",    )    initializeImageUploader(      "additionalImage3",      "additionalPhoto3Container",      "removeAdditionalImage3",      "additionalImage3Btn",      "oldAdditionalImage3",    )    if (rotateLeftBtn) {      rotateLeftBtn.addEventListener("click", () => {        if (cropper) cropper.rotate(-90)      })    }    if (rotateRightBtn) {      rotateRightBtn.addEventListener("click", () => {        if (cropper) cropper.rotate(90)      })    }    if (zoomInBtn) {      zoomInBtn.addEventListener("click", () => {        if (cropper) cropper.zoom(0.1)      })    }    if (zoomOutBtn) {      zoomOutBtn.addEventListener("click", () => {        if (cropper) cropper.zoom(-0.1)      })    }    if (resetBtn) {      resetBtn.addEventListener("click", () => {        if (cropper) cropper.reset()      })    }    if (cropImageBtn) {      cropImageBtn.addEventListener("click", () => {        if (!cropper || !currentImageInput || !currentImageContainer || !currentRemoveButton) return        const canvas = cropper.getCroppedCanvas({          width: 800,          height: 1000,          fillColor: "#fff",          imageSmoothingEnabled: true,          imageSmoothingQuality: "high",        })        if (!canvas) return        canvas.toBlob((blob) => {          const file = new File([blob], "cropped-image.png", { type: "image/png" })          const dataTransfer = new DataTransfer()          dataTransfer.items.add(file)          currentImageInput.files = dataTransfer.files          const imageUrl = URL.createObjectURL(blob)          currentImageContainer.innerHTML = ""          const img = document.createElement("img")          img.src = imageUrl          img.className = "image-preview"          currentImageContainer.appendChild(img)          currentRemoveButton.classList.remove("d-none")          if (currentOldImageInput) {            currentOldImageInput.setAttribute("data-replaced", "true")          }          clearError(currentImageInput)          cropperModal.hide()          cropper.destroy()          cropper = null        }, "image/png")      })    }    cropperModalEl.addEventListener("hidden.bs.modal", () => {      if (cropper) {        cropper.destroy()        cropper = null      }    })    const productForm = document.getElementById("updateProductForm")    if (productForm) {      productForm.addEventListener("submit", (e) => {        e.preventDefault()        clearAllErrors()        let isValid = true        const productName = document.getElementById("productName")        const description = document.getElementById("description")        const category = document.getElementById("category")        const color = document.getElementById("color")        const fabric = document.getElementById("fabric")        if (!productName.value.trim()) {          showError(productName, "Product name is required")          isValid = false        }        if (!description.value.trim()) {          showError(description, "Product description is required")          isValid = false        }        if (!category.value) {          showError(category, "Category selection is required")          isValid = false        }        if (!color.value.trim()) {          showError(color, "Product color is required")          isValid = false        }        if (!fabric.value.trim()) {          showError(fabric, "Fabric type is required")          isValid = false        }        const variantPrices = document.querySelectorAll('input[name^="varientPrice"]')        const variantQuantities = document.querySelectorAll('input[name^="sizes"]')        let hasValidVariant = false        for (let i = 0; i < variantPrices.length; i++) {          const price = variantPrices[i].value.trim()          const quantity = variantQuantities[i].value.trim()          if (price && quantity) {            hasValidVariant = true            break          }        }        if (!hasValidVariant) {          showError(variantPrices[0], "At least one variant must have price and quantity")          showError(variantQuantities[0], "At least one variant must have price and quantity")          isValid = false        }        const mainImage = document.getElementById("mainImage")        const oldMainImage = document.querySelector('input[name="oldMainImage"]')        const mainImageRemoved = oldMainImage && oldMainImage.getAttribute("data-removed") === "true"        const hasMainImage = (mainImage.files && mainImage.files.length > 0) || (oldMainImage && !mainImageRemoved)        if (!hasMainImage) {          document.getElementById("mainPhotoUpload").classList.add("is-invalid")          document.getElementById("mainImageError").textContent = "Main product image is required"          document.getElementById("mainImageError").classList.add("show")          isValid = false        }        if (isValid) {          const deletedImagesInput = document.createElement("input")          deletedImagesInput.type = "hidden"          deletedImagesInput.name = "deletedImages"          deletedImagesInput.value = JSON.stringify(deletedImages)          productForm.appendChild(deletedImagesInput)          productForm.submit()        } else {          const firstError = document.querySelector(".invalid-feedback.show")          if (firstError) {            firstError.scrollIntoView({ behavior: "smooth", block: "center" })          }        }      })    }  })  