<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Order Success - WEARIT</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/css/header.css" />
  <link rel="stylesheet" href="/css/footer.css" />
  <link rel="stylesheet" href="/css/order-success.css" />

  <style>
    .order-success-container {
      max-width: 800px;
      margin: 40px auto;
      padding: 20px;
    }
    .order-success-card {
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      padding: 30px;
      text-align: center;
      margin-bottom: 30px;
    }
    .success-icon {
      color: #2ecc71;
      font-size: 3rem;
      margin-bottom: 20px;
    }
    .order-id {
      color: #666;
      margin-bottom: 20px;
    }
    .order-detail-card {
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin-bottom: 20px;
    }
    .order-detail-header {
      border-bottom: 1px solid #eee;
      padding-bottom: 15px;
      margin-bottom: 15px;
    }
    .order-actions {
      display: flex;
      justify-content: space-between;
      margin-top: 30px;
    }
    .btn-view-orders {
      background-color: #000;
      color: #fff;
      padding: 10px 20px;
      border-radius: 5px;
      text-decoration: none;
      transition: all 0.3s;
    }
    .btn-view-orders:hover {
      background-color: #333;
      color: #fff;
    }
    .btn-continue-shopping {
      background-color: transparent;
      color: #000;
      border: 1px solid #000;
      padding: 10px 20px;
      border-radius: 5px;
      text-decoration: none;
      transition: all 0.3s;
    }
    .btn-continue-shopping:hover {
      background-color: #f5f5f5;
    }
    .wallet-info {
      margin-top: 15px;
      padding-top: 15px;
      border-top: 1px dashed #eee;
    }
    .wallet-badge {
      display: inline-block;
      background-color: #f8f9fa;
      border: 1px solid #e9ecef;
      border-radius: 4px;
      padding: 2px 8px;
      font-size: 0.85rem;
      margin-left: 5px;
    }
    .text-success {
      color: #21aa1e !important;
    }
    .text-danger {
      color: #c12121 !important;
    }
  </style>
</head>

<body>
  <%- include('../partials/header') %>

  <div class="order-success-container">
    <div class="order-success-card">
      <i class="bi bi-check-circle-fill success-icon"></i>
      <h2>Order Placed Successfully!</h2>
      <p class="order-id">Order ID: <%= order.orderID || order._id %></p>
      <p>Thank you for shopping with us. We've received your order and will process it shortly.</p>
    </div>

    <div class="order-detail-card">
      <div class="order-detail-header">
        <h4>Order Details</h4>
      </div>

      <div class="row">
        <div class="col-md-6">
          <h5>Shipping Address</h5>
          <% if (order.address) { %>
          <p>
            <%= order.address.name %><br>
            <%= order.address.address || order.address.addressLine1 %><br>
            <%= order.address.city %>, <%= order.address.state %> - <%= order.address.pincode %><br>
            Phone: <%= order.address.mobile %>
          </p>
          <% } else { %>
          <p>Address information not available</p>
          <% } %>
        </div>
        <div class="col-md-6">
          <h5>Payment Information</h5>
          <p>
            Method: 
            <% if ((order.paymentMethod || order.paymentMentod) === 'wallet') { %>
              Wallet Payment <span class="wallet-badge"><i class="bi bi-wallet2"></i> Wallet</span>
            <% } else if ((order.paymentMethod || order.paymentMentod) === 'COD') { %>
              Cash on Delivery
            <% } else if ((order.paymentMethod || order.paymentMentod) === 'card') { %>
              Credit/Debit Card
            <% } else if ((order.paymentMethod || order.paymentMentod) === 'upi') { %>
              UPI Payment
            <% } else if ((order.paymentMethod || order.paymentMentod) === 'bank') { %>
              Bank Transfer
            <% } else { %>
              <%= (order.paymentMethod || order.paymentMentod) || 'Online Payment' %>
            <% } %>
            <br>
            Amount: ₹<%= order.finalAmount.toFixed(2) %>
          </p>

          <% if ((order.paymentMethod || order.paymentMentod) === 'wallet') { %>
          <div class="wallet-info">
            <p class="mb-1"><strong>Wallet Transaction Details:</strong></p>
            <p class="mb-1 text-danger">
              <i class="bi bi-arrow-down-circle"></i> ₹<%= order.finalAmount.toFixed(2) %> debited from your wallet
            </p>
            <% if (user && user.wallet) { %>
            <p class="mb-0">
              <i class="bi bi-wallet2"></i> Remaining Balance: <strong>₹<%= user.wallet.balance.toFixed(2) %></strong>
            </p>
            <p class="mt-2 mb-0">
              <a href="/wallet" class="btn btn-sm btn-outline-dark">View Wallet <i class="bi bi-arrow-right"></i></a>
            </p>
            <% } %>
          </div>
          <% } %>
        </div>
      </div>
    </div>

    <div class="order-actions">
      <a href="/" class="btn-continue-shopping">Continue Shopping</a>
      <a href="/orders/<%= order._id %>/track" class="btn-view-orders">Track Order</a>
    </div>
  </div>

  <%- include('../partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="/js/header.js"></script>
</body>

</html>